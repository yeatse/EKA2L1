# EKA2L1 iOS frontend (work in progress)
#
# At stage 0 this is just a placeholder so the top-level CMake graph resolves
# on iOS. The real Objective-C++ bridge layer and the Swift/UIKit app are
# introduced in task 0.6. Bringing actual targets up here too early would
# prematurely pull in symbols from emu modules that have not been ported yet.
message(STATUS "EKA2L1 iOS frontend: stage-0 placeholder (no targets yet)")
